/**
  @file a.cpp
  @title A - 複数形
  @url https://atcoder.jp/contests/abc029/tasks/abc029_a
**/

#include <bits/stdc++.h>
using namespace std;

typedef long long LL;
#define ALL(obj) (obj).begin(), (obj).end()
#define REP(i, N) for (int i = 0; i < (N); ++i)

int main() {
  string W;
  cin >> W;

  cout << W + "s" << endl;
  return 0;
}
